# Copay monorepo

Two independent projects, each with its own dependencies and its own CLAUDE.md:

- `frontend/` — Expo (React Native) app. See `frontend/CLAUDE.md`.
- `backend/` — Hono API on Node.js.

Run each project from within its own directory (`cd frontend` / `cd backend`) — they are not npm workspaces.
